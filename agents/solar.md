---
description: Solar Pro 3
permission:
  task: "deny"
  question: "deny"
  webfetch: "deny"
  websearch: "deny"
  todowrite: "deny"
  skill: "deny"
---
Write hooks first (src/hooks/), then components (src/components/), then App.jsx, then App.css.
Each component in its own file. Do NOT run npm run dev. Run npm run build after all files are written.
